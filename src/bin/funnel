#!/bin/sh
#
## ---------------------------------------------------------------------
## ---------------------------------------------------------------------
. /opt/funnel/etc/conf
if [ -a ${homedir}/funnel/etc/conf ]
then
   . ${homedir}/funnel/etc/conf
fi

## ---------------------------------------------------------------------
## ---------------------------------------------------------------------
java -Dversion=${artifact.version} ${JVM_ARGS} -jar ${JVM_RUNNABLE_JAR} ${FUNNEL_OPTS} $@
